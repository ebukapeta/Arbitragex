/**
 * Production start script — used by Render Web Service
 * Starts the Express server which serves both the API and the built React frontend.
 *
 * Run: node server/index.js
 * Or via npm: npm start (after adding "start": "node server/index.js" to package.json)
 */

// This file is just a pointer — actual entry is server/index.js
// Run: node server/index.js
